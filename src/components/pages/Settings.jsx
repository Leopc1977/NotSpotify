import { observer } from "mobx-react-lite";
import styled from "styled-components";

const TitleStyled = styled.div``;

function Settings() {
  return <TitleStyled>Settings</TitleStyled>;
}

export default observer(Settings);
